# Contributing

Keep the enforcement path deterministic and dependency-free. Every parser, graph or time-boundary change needs positive, negative and boundary tests. Run `PYTHONPATH=src python -m unittest discover -s tests -v`.
